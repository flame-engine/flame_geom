name: flame_geom
description: Complement Flame with APIs for handling shapes and geometric operations.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flame: ^0.21.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter